import styled from "styled-components";

// 현재 메인 페이지에서 전체 박스로 참조 되고있음
const MainStyle = styled.div`
  max-width: 1920px;
  max-height: 1080px;
  margin: 0 auto;
  position: relative;
`;

export default MainStyle;
